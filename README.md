# PH-assignment05




# Project name: Technologies Stack
#description: 
this is a project, where we can manage our technologies stack and add technologies stack from technology of group. which 
technologies will selected those are showing right site, and we can remove itself single and also all of those at a time with a button.







# Technologies i have used for it:

* Reactjs, typescript, tailwind, react toastify, json for data etc




# What is JSX, and why is it used in React?

answer: JSX stands for JavaScript XML. It allows us to write HTML-like syntax inside JavaScript.

#  What is the difference between props and state?
answer: #props:- props used to send data parent components to child, #state:- state is a function which manage data for Reactjs components.


# What does the useState hook do, and where did you use it in this project?

answer: useState is a hook of reactjs, where we can store data and change it as we need.



# What does the useEffect hook do, and why did you need it to load the JSON data?
answer: useEffect is a hook of react side Effect, it's fetch data when page is loaded. or we can take action based on any specific something change execution as use useEffect defendecies.




# Why does every item in a .map() list need a unique key prop?
answer: because of, map create a new array, and those all are elment need a unique key prop for specificly idendify them.



# What is conditional rendering? Show one place you used it. 
answer: conditional rendering means data render by condition, i have used it on this project's techonology add to stack button.  there i have used conditional render, when it will add to stack button click it will render Added


# How do you pass data from a parent component to a child component, and how does a child send something back to the parent?

answer: react doesn't send data child => parent, but also can parent => child pass any data as a component props.
and i can send something back to parent from child through the parent function params as it pass to child component's props. that's means i can send back a data child to parent with function params as a props.